---
description: Show durable state for one subtitle title
agent: subtitle-orchestrator
---

For project `$1` title `$2`, run `subflow status $1 $2` and summarize only actionable blockers and the next valid command. Do not infer stage from chat history.
