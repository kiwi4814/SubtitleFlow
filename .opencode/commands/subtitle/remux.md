---
description: Remux approved subtitle tracks into the target MKV
agent: subtitle-orchestrator
---

For project `$1` title `$2`, verify a frozen release exists and QA passed. Use `subflow remux $1 $2` with the user's configured/provided video and output path. Do not transcode video or audio. Report the exact output only after `mkvmerge` succeeds.
