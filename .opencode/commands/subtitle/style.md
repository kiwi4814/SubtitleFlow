---
description: Inspect or select the versioned ASS style profile for a title
agent: subtitle-orchestrator
---

For project `$1` title `$2`, run `subflow style show $1 $2`. If `$3` names a profile, set it with `subflow style set $1 $2 $3`, then explain that compiled/QA/visual/release stages became stale and must be rerun. Never alter source ASS styles in place.
