---
description: Verify sources, normalize, align, and seed both release branches
agent: subtitle-orchestrator
---

For project `$1` title `$2`, run source verification and `subflow prepare $1 $2`. Report A↔D, A↔B, and JP↔C alignment summaries, low-confidence counts, missing evidence, and any context-sensitive terminology flags. Do not perform semantic rewrites yet.
