---
description: Verify sources, normalize, and build only the branches supported by the current evidence profile
agent: subtitle-orchestrator
---

For project `$1` title `$2`, verify source hashes and run `subflow prepare $1 $2`. Read the configured workflow profile and active branches first. Report only alignment that actually occurred. `single` has no alignment; `source-assisted` uses S timing and C semantic evidence. Do not perform semantic rewrites yet.
